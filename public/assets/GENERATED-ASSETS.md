# ⚠️ AI-generated placeholder images — replace with real product output

These images were generated with an AI model (Higgsfield / Recraft 4.1) as
temporary placeholders because no real asset existed for the slot. **Swap them
for real ArchLab renders or product captures as soon as possible.**

| File | Used on | Slot | Replace with |
|------|---------|------|--------------|
| `generated-interior-placeholder.webp` | Homepage | "Room Redecoration" use-case card | A real ArchLab interior render |
| `generated-exterior-placeholder.webp` | Homepage | "Renovation Visuals" use-case card | A real ArchLab exterior render |

## Still needed (could not be generated authentically)
- **Hero sketch→render video/GIF** — a real screen recording from `client.archlab.pro` (autoplay, muted loop). The best single conversion lift; only a genuine capture should be used here.

Everything else on the homepage uses **real** repo assets (`sketch-before`,
`render-after`, `ai-interface-render`, `ai-interface-analysis`,
`cad-transformation`, `floor-plan-sketch`).
